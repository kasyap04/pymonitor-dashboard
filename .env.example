# Copy to .env and adjust values

# Collector base URL — used in production builds.
# In dev, Vite proxies /collector → localhost:8888 (see vite.config.ts).
VITE_COLLECTOR_URL=http://your-collector-host:8888
